import { DestinationProps } from "./LocationTypes";


const Destinations = ({editMode, destinations, setDestinations, tripId}: DestinationProps) => {

    console.log(destinations);
    return (
        <>
        </>
    )
}

export default Destinations;